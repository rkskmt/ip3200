---
title: "東京都オープンデータAPI"
subtitle: "自治体データを活用する"
---

## {background-color="white" .center .break-slide}

::: {.break-title}
なぜオープンデータか
:::


## オープンデータとは

- 政府・自治体が **誰でも自由に使える形式** で公開するデータ
- 商用・非商用問わず **二次利用可能**
- 機械可読（CSV, JSON, API）で提供

::: {.callout-note}
## 意義
- **透明性**：行政の活動を市民が検証できる
- **イノベーション**：民間がデータを活用してサービス開発
- **効率化**：データに基づく政策立案（**EBPM**: [**E**vidence-**B**ased **P**olicy **M**aking]{.en}）
:::


## 身近なオープンデータ活用例

| サービス | 使用データ |
|---|---|
| 乗換案内アプリ | 公共交通の時刻表・遅延情報 |
| 防災アプリ | 避難所・ハザードマップ |
| 不動産サイト | 地価公示・用途地域 |
| 天気予報 | 気象庁の観測データ |

- **すでに使っている** — 意識していないだけ
- 自分でもAPIを叩けば **同じデータ** が手に入る


## {background-color="white" .center .break-slide}

::: {.break-title}
東京都オープンデータAPI
:::


## 概要

- **東京都オープンデータカタログサイト** の一部データをAPI提供
- ユーザ登録 **不要** — 誰でもすぐに利用可能
- 掲載データ約54,000件のうち、CSV約35,000件がAPI対応

| 項目 | 内容 |
|---|---|
| 認証 | 不要 |
| レート制限 | 明示なし（常識的な範囲で） |
| データ形式 | JSON / XML |
| CORS | 対応済み |


## Wikipedia APIとの違い

| | Wikipedia API | 東京都API |
|---|---|---|
| HTTPメソッド | **GET** | **POST** |
| データ内容 | 百科事典・統計 | 行政・地理・施設 |
| 更新頻度 | リアルタイム〜日次 | 不定期（年次も多い） |

::: {.callout-warning}
## 重要：POSTでアクセス
- Wikipedia API は `requests.get()`
- 東京都API は `requests.post()`
- 間違えると動かない
:::


## {background-color="white" .center .break-slide}

::: {.break-title}
APIの構造
:::


## 基本URL

```text
https://service.api.metro.tokyo.lg.jp/api/{apiId}/{format}
```

- `{apiId}` — データセットごとに異なるID
- `{format}` — `json` または `xml`

例：東京都人口の推移（JSON）

```text
https://service.api.metro.tokyo.lg.jp/api/t000003d2000000236-c054eb26d9e9721642bef78c7c717e76-0/json
```


## APIのIDを探す

- [API検索ページ](https://spec.api.metro.tokyo.lg.jp/spec/search) でキーワード検索
- 欲しいデータの apiId をコピーして使う

::: {.callout-tip}
## 探し方のコツ
- 「避難所」「駐輪場」「人口」などキーワードで検索
- 区市町村名で絞り込み可能
- 詳細ページで apiId と列名を確認
:::


## パラメータ

| パラメータ | 説明 | 例 |
|---|---|---|
| `limit` | 取得レコード数の上限 | `params = {"limit": 200}` |
| `offset` | 取得開始位置 | `params = {"offset": 50}` |

- デフォルトは **100件** — 全件欲しいときは `limit` を大きく設定
- 取得する **列の絞り込み** は、URLではなく **POSTのbody** で指定する（後述）


## {background-color="white" .center .break-slide}

::: {.break-title}
基本的な使い方
:::


## 最小テンプレート

```python
import requests

api_id = "t000003d2000000236-c054eb26d9e9721642bef78c7c717e76-0"
url = f"https://service.api.metro.tokyo.lg.jp/api/{api_id}/json"

params = {"limit": 200}
headers = {"User-Agent": "example.com/lesson"}
body = {}

response = requests.post(url, params=params, headers=headers, json=body)
data = response.json()

print(data["hits"])
```

::: {.callout-note}
## `json={}` の意味
- 空のJSONボディを送信している — 東京都APIの仕様上、省略不可
- 省略すると **415**（Unsupported Media Type）が返ってくる — 400番台＝リクエスト側のミス、の実例
:::


## レスポンス構造

```json
{
  "total": 152,
  "subtotal": 152,
  "limit": 200,
  "offset": null,
  "metadata": {
    "apiId": "API識別子",
    "title": "データタイトル",
    "version": "1.0.7"
  },
  "hits": [
    {"row": 1, "和暦（年次）": "明治5", "西暦（年次）": 1872, "東京都／人口": 859345},
    {"row": 2, "和暦（年次）": "明治6", "西暦（年次）": 1873, "東京都／人口": 887322},
    ...
  ]
}
```

- `total` — 全件数
- `metadata` — データセットの情報
- `hits` — **実際のデータ本体**（list of dict）


## {background-color="white" .center .break-slide}

::: {.break-title}
検索条件を指定する
:::


## 列を絞る・行を絞る

- **列** の限定は、POSTのbodyに `column` を入れる
- **行** の絞り込み（期間指定など）は、取得後に **Pythonでフィルタ** するのが確実
   - 内包表記の `if` — [Pandas入門](pandas.qmd)の条件抽出と同じ発想

```python
# 列を限定して取得
body = {
    "column": ["西暦（年次）", "東京都／人口"]
}

# 取得後に Python でフィルタリング
filtered = [r for r in data["hits"]
            if 1950 <= r["西暦（年次）"] <= 2023]
```


## {background-color="white" .center .break-slide}

::: {.break-title}
実用サンプル
:::


## 東京都人口（期間指定）

```python
import requests

headers = {"User-Agent": "example.com/lesson"}
api_id = "t000003d2000000236-c054eb26d9e9721642bef78c7c717e76-0"
url = f"https://service.api.metro.tokyo.lg.jp/api/{api_id}/json"

params = {"limit": 200}
body = {"column": ["西暦（年次）", "東京都／人口"]}

response = requests.post(url, params=params, headers=headers, json=body)
data = response.json()

# Python でフィルタリング
filtered = [r for r in data["hits"]
            if 1950 <= r["西暦（年次）"] <= 2023]

print(f"総件数: {len(filtered)}")
for record in filtered[:5]:
    print(f"{record['西暦（年次）']}年: {record['東京都／人口']:,}人")
```

```text
総件数: 74
1950年: 6,277,500人
1951年: 6,712,494人
1952年: 7,108,749人
1953年: 7,468,907人
1954年: 7,773,648人
```


## 人口推移の可視化

- さっきの `filtered` から列を取り出して、いつもの `plt.plot()` に渡すだけ

```{python}
#| echo: true
#| eval: false
#| code-fold: true
#| code-summary: "コードを見る"
import numpy as np
import matplotlib.pyplot as plt
import japanize_matplotlib

years      = np.array([r["西暦（年次）"] for r in filtered])
population = np.array([r["東京都／人口"] for r in filtered])

plt.plot(years, population / 1e6)
plt.title("Tokyo Population 1950-2023")
plt.xlabel("Year")
plt.ylabel("Population (million)")
plt.grid(True, linestyle="--", alpha=0.5)
plt.show()
```

```{python}
#| echo: false
#| fig-align: center
import numpy as np
import matplotlib.pyplot as plt
import japanize_matplotlib

pop = np.loadtxt("data/tokyo_population.csv", delimiter=",", skiprows=1)
mask = pop[:, 0] >= 1950
years, population = pop[mask, 0], pop[mask, 1]

fig, ax = plt.subplots(figsize=(6, 3))
ax.plot(years, population / 1e6, color="#2266CC", linewidth=1.2)
ax.set_title("Tokyo Population 1950-2023")
ax.set_xlabel("Year")
ax.set_ylabel("Population (million)")
ax.grid(True, linestyle="--", alpha=0.5)
plt.tight_layout()
plt.show()
```

- 戦後復興期から高度経済成長期にかけて **急増**
- 1970年代からしばらく **横ばい**、1990年代後半から再び増加
- このように **まず描く** ことで傾向が見える


## 避難所情報（品川区）

```python
import requests

headers = {"User-Agent": "example.com/lesson"}
api_id = "t131091d0000000001-3f9091b43cf1caa550e7152b5c2ba34c-0"
url = f"https://service.api.metro.tokyo.lg.jp/api/{api_id}/json"

params = {"limit": 5}
body = {}

response = requests.post(url, params=params, headers=headers, json=body)
data = response.json()

print(f"品川区の避難所情報（総件数: {data['total']}件）")

for facility in data["hits"]:
    print("---")
    for key, value in facility.items():
        if key != "row":
            print(f"{key}: {value}")
```

```text
品川区の避難所情報（総件数: 25件）
---
名称: 品川区立第一日野小学校
緯度: 35.6234...
経度: 139.7312...
---
...
```


## 避難所リスト（東京都全域）

```python
import requests

headers = {"User-Agent": "example.com/lesson"}
api_id = "t000003d0000000093-0af9bfa497bc7be6ef83e5bfd1fdcfc6-0"
url = f"https://service.api.metro.tokyo.lg.jp/api/{api_id}/json"

params = {"limit": 5}
body = {}

response = requests.post(url, params=params, headers=headers, json=body)
data = response.json()

print(f"東京都の避難所（総件数: {data['total']}件）")
for shelter in data["hits"]:
    print(f"施設名: {shelter['避難所_施設名称']}")
    print(f"住所: {shelter['所在地住所']}")
    print(f"区市町村: {shelter['指定市区町村名']}")
    print("---")
```

```text
東京都の避難所（総件数: 2636件）
施設名: お茶の水小学校
住所: 東京都千代田区神田猿楽町1-1-1
区市町村: 千代田区
---
施設名: 都立一橋高校
住所: 東京都千代田区東神田1-12-13
区市町村: 千代田区
---
...
```


## 都営バス運賃

```python
import requests

headers = {"User-Agent": "example.com/lesson"}
api_id = "t000018d0000000044-3db21a77b0af1c54b411f79604444ca2-0"
url = f"https://service.api.metro.tokyo.lg.jp/api/{api_id}/json"
body = {}

response = requests.post(url, headers=headers, json=body)
data = response.json()

print(f"都営バス運賃情報（総件数: {data['total']}件）")

for fare_info in data["hits"][:5]:
    print("---")
    for key, value in fare_info.items():
        if key != "row":
            print(f"{key}: {value}")
```

```text
都営バス運賃情報（総件数: 4件）
---
区分: 一般系統
大人: 210円（IC 210円）
小児: 110円（IC 105円）
備考: 急行バス、直行バスを含む。
---
...
```


## {background-color="white" .center .break-slide}

::: {.break-title}
NumPyで統計処理
:::


## 統計分析の例

- `hits` は **list of dict** — 内包表記で列を取り出して `np.array` にすれば、いつもの統計がそのまま使える

```python
import requests
import numpy as np

headers = {"User-Agent": "example.com/lesson"}
api_id = "t000003d2000000236-c054eb26d9e9721642bef78c7c717e76-0"
url = f"https://service.api.metro.tokyo.lg.jp/api/{api_id}/json"

params = {"limit": 200}
response = requests.post(url, params=params, headers=headers, json={})
data = response.json()

population = np.array([r["東京都／人口"] for r in data["hits"]])

print(f"平均: {np.mean(population):,.0f}人")
print(f"最大: {np.max(population):,}人")
print(f"最小: {np.min(population):,}人")
print(f"標準偏差: {np.std(population):,.0f}")
print(f"中央値: {np.median(population):,.0f}人")
```

```text
平均: 7,294,170人
最大: 14,099,993人
最小: 859,345人
標準偏差: 4,567,885
中央値: 7,189,875人
```


## 実データは汚れている

- 人口の列は綺麗だったが、同じ表の **増減率(%)** の列を見ると…

```python
rates_raw = [r["東京都／増減率(%)"] for r in data["hits"]]
print(rates_raw[:4])
```

```text
['…', '3.26', '5.09', '5.75']
```

- 数値が **文字列**（`"3.26"`）で入っていて、データがない年は `"…"`
- 使うには、`"…"` を除いてから `float()` で数値に変換する

```python
rates = np.array([float(v) for v in rates_raw if v != "…"])
print(f"増減率の平均: {rates.mean():.2f}%")   # 増減率の平均: 2.10%
```

::: {.callout-tip}
## 型を確かめてから使う
- 外部APIのデータは **型が揃っているとは限らない** — 数値のつもりが文字列、欠損が `"…"` などの記号
- 集計の前に `print(type(値))` や `isinstance(値, int)` で確認するクセをつける
:::


## まずは可視化する

- 数値だけでは傾向がわからない
- [データの可視化](visualize.qmd)の大原則：**Always plot your data first.**

```{python}
#| echo: false
#| fig-align: center
import numpy as np
import matplotlib.pyplot as plt
import japanize_matplotlib

pop = np.loadtxt("data/tokyo_population.csv", delimiter=",", skiprows=1)
years, population = pop[:, 0], pop[:, 1]

fig, ax = plt.subplots(figsize=(7, 3))
ax.plot(years, population / 1e6, color="#2266CC", linewidth=1.2)
ax.fill_between(years, population / 1e6, alpha=0.3, color="#2266CC")
ax.set_title("Tokyo Population 1872-2023")
ax.set_xlabel("Year")
ax.set_ylabel("Population (million)")
ax.grid(True, linestyle="--", alpha=0.5)
plt.tight_layout()
plt.show()
```

- **右肩上がり** — 150年で約16倍に増加
- **1940年代に急落** — 戦争による疎開・被害
- **2020年代は横ばい** — コロナ禍の2021年には減少もあった

::: {.callout-tip}
## これまでの道具が活きる
- [listあらためて](list.qmd)の内包表記で抽出
- [NumPy入門](numpy.qmd)の統計関数で集計
- [データの可視化](visualize.qmd)・[matplotlib入門](matplotlib.qmd)の手法で可視化
:::


## まとめ

| やりたいこと | コード |
|---|---|
| 基本アクセス | `requests.post(url, params=..., headers=..., json={})` |
| 取得件数指定 | `params = {"limit": 200}` |
| 列の絞り込み | `body = {"column": ["列名1", "列名2"]}` |
| 行の絞り込み | 取得後にPythonでフィルタ（内包表記 + `if`） |
| 全件数確認 | `data["total"]` |
| データ本体 | `data["hits"]`（list of dict） |

::: {.callout-warning}
## Wikipedia API との違い
- **GET** ではなく **POST**
- 空でも `json={}` が必要
:::


## {background-color="white" .center .break-slide}

::: {.break-title}
演習
:::


## 演習：避難所データの可視化 {.badge-practice}

::: {.prompt}
任意の区市町村の**避難所データ**を取得し、**件数**を表示せよ。
:::

- （発展）緯度・経度があれば散布図で地図風にプロット

::: {.callout-tip}
## ヒント
- [API検索ページ](https://spec.api.metro.tokyo.lg.jp/spec/search) で「避難所」を検索
- 複数の区のデータを比較しても面白い
:::

```{python}
#| echo: true
#| eval: false
#| code-fold: true
#| code-summary: "解答例を見る（品川区の例 — apiIdは検索ページで差し替え）"
import requests

headers = {"User-Agent": "example.com/lesson"}
api_id = "t131091d0000000001-3f9091b43cf1caa550e7152b5c2ba34c-0"  # 品川区の避難所
url = f"https://service.api.metro.tokyo.lg.jp/api/{api_id}/json"

response = requests.post(url, headers=headers, json={})
data = response.json()

print(f"避難所の件数: {data['total']}件")
```


## 演習：人口推移の時系列グラフ {.badge-practice}

::: {.prompt}
東京都の人口データを**全件**取得し、人口推移を折れ線グラフで描け。
:::

1. 年と人口を取り出して **折れ線グラフ** を描画
2. **最大値・最小値・平均** をタイトルに表示

```{python}
#| echo: true
#| eval: false
#| code-fold: true
#| code-summary: "解答例を見る"
import requests
import numpy as np
import matplotlib.pyplot as plt
import japanize_matplotlib

headers = {"User-Agent": "example.com/lesson"}
api_id = "t000003d2000000236-c054eb26d9e9721642bef78c7c717e76-0"
url = f"https://service.api.metro.tokyo.lg.jp/api/{api_id}/json"

params = {"limit": 200}
response = requests.post(url, params=params, headers=headers, json={})
data = response.json()

years      = np.array([r["西暦（年次）"] for r in data["hits"]])
population = np.array([r["東京都／人口"] for r in data["hits"]])

plt.figure(figsize=(10, 4))
plt.plot(years, population / 1e6)
plt.title(f"Max={np.max(population):,}, Min={np.min(population):,}, Mean={np.mean(population):,.0f}")
plt.xlabel("Year")
plt.ylabel("Population (million)")
plt.grid(True, linestyle="--", alpha=0.5)
plt.show()
```


## 演習：2つのデータセットを比較 {.badge-practice}

::: {.prompt}
**異なる2つのデータセット**（例：2つの区の避難所、戦前と戦後の人口）を取得し、件数や統計量を比較して表示せよ。
:::

- （発展）同じグラフに重ねて描画

```{python}
#| echo: true
#| eval: false
#| code-fold: true
#| code-summary: "解答例を見る（戦前と戦後の人口を比較する例）"
import requests
import numpy as np

headers = {"User-Agent": "example.com/lesson"}
api_id = "t000003d2000000236-c054eb26d9e9721642bef78c7c717e76-0"
url = f"https://service.api.metro.tokyo.lg.jp/api/{api_id}/json"

response = requests.post(url, params={"limit": 200}, headers=headers, json={})
hits = response.json()["hits"]

prewar  = np.array([r["東京都／人口"] for r in hits if r["西暦（年次）"] <= 1944])
postwar = np.array([r["東京都／人口"] for r in hits if r["西暦（年次）"] >= 1946])

print(f"戦前（〜1944）: {len(prewar)}年分 平均 {prewar.mean():,.0f}人")
print(f"戦後（1946〜）: {len(postwar)}年分 平均 {postwar.mean():,.0f}人")
```


## {background-color="white" .center .break-slide}

::: {.break-title}
活用アイデア
:::


## アプリケーション例

| 分野 | 活用例 |
|---|---|
| **防災** | 避難所マップ、消火栓・防火水槽の位置、地震被害想定 |
| **交通** | 駐輪場検索、バス運賃計算、駅の乗降客数分析 |
| **生活** | 公衆トイレマップ、公共施設案内、子育て支援制度 |
| **研究** | 人口動態分析、地価変動追跡、行政サービス利用調査 |


## 関連リソース

| リソース | URL |
|---|---|
| オープンデータカタログサイト | [portal.data.metro.tokyo.lg.jp](https://portal.data.metro.tokyo.lg.jp/) |
| APIカタログサイト | [portal.data.metro.tokyo.lg.jp/opendata-api/](https://portal.data.metro.tokyo.lg.jp/opendata-api/) |
| API検索ページ | [spec.api.metro.tokyo.lg.jp/spec/search](https://spec.api.metro.tokyo.lg.jp/spec/search) |
| 公共交通オープンデータ | [odpt.org](https://www.odpt.org/) |
| デジタル庁 GIF | [digital.go.jp](https://www.digital.go.jp/policies/data_strategy_government_interoperability_framework) |
