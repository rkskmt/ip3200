---
title: "変数と代入"
subtitle: "メモリモデルで理解する"
---

## 事件：関数に渡しただけなのに

成績データの「2倍にしたコピー」を作ろうとした。

```python
def double_all(data):
    for i in range(len(data)):
        data[i] = data[i] * 2
    return data

scores = [10, 20, 30]
result = double_all(scores)
print(result)  # [20, 40, 60] ← 期待どおり
print(scores)  # [20, 40, 60] ← ！？ 元データまで壊れた
```

- `scores` には一度も代入していないのに、**元の成績が消えた**

::: {.callout-important}
## 今回のゴール
この回が終わる頃には、この事故を**図1枚で説明**でき、**1行で防げる**ようになる。
:::


## リテラル（値）

- 事故の正体を突き止めるために、敢えて「変数と代入」をメモリモデルからおさらいする
- Pythonでは**値**を直接コードに書ける
   - 値はそれだけで評価できる（エラーにならない）
   - 変数や関数なしで、その行に書くだけで意味を持つ

```python
1
"1"
[1, 2, 3]
True
1.0
```

:::{.callout-note}
## メモリ上での記述

上記の値はその行が実行された時に、**メモリ上に値として記述**される（`000001001` のようにメモリ上のスペースを使って記録される）。これらを**リテラル**と呼ぶ — 直接コード中に記述できる値の表現方法。
:::

## 変数とは

一方、このように書くとエラーになる。なぜ？

```python
a # NameError: name 'a' is not defined
```

- `1` や `"1"` は**リテラル（直接記述できる値）**
- `a` は値ではなく**変数**として認識される

```{python}
#| echo: false
#| fig-align: center
import matplotlib.pyplot as plt
import japanize_matplotlib

TAG_FC, TAG_EC = "#fef9e7", "#b7950b"
OBJ_FC, OBJ_EC = "#d6eaf8", "#2471a3"
FADE_FC, FADE_EC = "#f2f3f4", "#aab7c4"
NEW_EC = "#c0392b"

def tag(ax, x, y, name, faded=False):
    ax.text(x, y, name, ha="center", va="center", fontsize=12, family="monospace",
            fontweight="bold", color="#7d6608" if not faded else "#aab7c4",
            bbox={"boxstyle": "round,pad=0.35",
                  "fc": TAG_FC if not faded else FADE_FC,
                  "ec": TAG_EC if not faded else FADE_EC, "lw": 1.8})

def obj(ax, x, y, value, addr, faded=False, hot=False):
    ec = NEW_EC if hot else (FADE_EC if faded else OBJ_EC)
    fc = FADE_FC if faded else OBJ_FC
    color = "#aab7c4" if faded else "#1c2833"
    ax.text(x, y, value, ha="center", va="center", fontsize=12, family="monospace",
            fontweight="bold", color=color,
            bbox={"boxstyle": "round,pad=0.45", "fc": fc, "ec": ec, "lw": 2})
    ax.text(x, y - 0.62, addr, ha="center", va="top", fontsize=8.5,
            color="#aab7c4" if faded else "#5d6d7e")

def link(ax, x0, y0, x1, y1, faded=False, hot=False):
    color = NEW_EC if hot else ("#d5d8dc" if faded else "#566573")
    ax.annotate("", xy=(x1, y1), xytext=(x0, y0),
                arrowprops={"arrowstyle": "->", "color": color, "lw": 2.2})

def panel(ax, title=None, mono=True):
    ax.set_xlim(0, 4.6); ax.set_ylim(0, 4); ax.axis("off")
    if title:
        ax.set_title(title, fontsize=11, family="monospace" if mono else None)

fig, axes = plt.subplots(1, 2, figsize=(9.5, 2.6))
ax = axes[0]
panel(ax, mono=False)
ax.set_title("看板のない値：すぐ回収されて消える", fontsize=11)
obj(ax, 2.3, 2.2, "1234", "誰にも指されていない", faded=True)
ax = axes[1]
panel(ax, mono=False)
ax.set_title("看板が立った値：生き続ける", fontsize=11)
tag(ax, 0.8, 2.2, "a")
obj(ax, 3.2, 2.2, "1234", "a に指されている")
link(ax, 1.35, 2.2, 2.45, 2.2)
plt.tight_layout()
plt.show()
```

- 変数とは、**値をつかんで離さないための「名前」** — 看板が立っている限り値は消えない
- `a` はまだどこにも立てていない看板なので、エラーになる

## 変数の命名規則

変数として認識される単語のルールは以下のとおり。

**例外はない — ここは覚えるしかない**

- 文字（a-z, A-Z）またはアンダースコア（_）で**始まる**
  - つまり数字始まりはダメ（NG例： 3200scores ）
  - 終わるのはOK（例：scores_of_3200）
- 2文字目以降が文字、数字（0-9）、アンダースコアであること
- 予約語（**`if` / `for` / `while`** など）ではないこと


```python
a01    # OK。変数
_aa_aa # OK。変数
1a     # NG。最初が数字
continue # NG。予約語
```

## 代入

- **変数 = リテラル** と記述すると「**代入**」という操作がおこなわれる
   - 左辺は変数
   - 右辺はリテラルかオブジェクト

```python
a = 1234
test_2 = "test2"
```

:::{.callout-tip}
## 箱ではなく「看板」
- `a = 1234` は「`a` という箱に `1234` が入る」**ではない**
- **`a` は `1234` が格納された住所（アドレス）を指す看板**になる
- ただし、アクセス時は**その看板自体ではなく住所が指す先にあるリテラルやオブジェクトが返る**
:::

```{python}
#| echo: false
#| fig-align: center
fig, axes = plt.subplots(1, 2, figsize=(9.5, 2.8))
ax = axes[0]
panel(ax, mono=False)
ax.set_title("× 箱モデル：a という箱に値が入る", fontsize=11)
ax.add_patch(plt.Rectangle((1.5, 1.2), 1.7, 1.6, fc="#fdf2e9", ec="#b9770e", lw=2))
ax.text(2.35, 3.1, "a", ha="center", fontsize=13, family="monospace",
        fontweight="bold")
ax.text(2.35, 2.0, "1234", ha="center", va="center", fontsize=13,
        family="monospace")
ax.plot([1.3, 3.4], [1.0, 3.0], color="#c0392b", lw=3)
ax.plot([1.3, 3.4], [3.0, 1.0], color="#c0392b", lw=3)
ax = axes[1]
panel(ax, mono=False)
ax.set_title("○ 看板モデル：a は置き場所を指す", fontsize=11)
tag(ax, 0.8, 2.0, "a")
obj(ax, 3.2, 2.0, "1234", "アドレス …832")
link(ax, 1.35, 2.0, 2.45, 2.0)
plt.tight_layout()
plt.show()
```

## 変数 = 変数 の代入

- **変数同士の代入**は、左辺に右辺のアドレスを代入する
   - つまり、**同じアドレス**を指すようになる

```python
a11 = 11
a12 = 12
a11 = a12 # a11とa12は同じアドレスを指す
print(a12) # 12

a11 = 13 # 別の値に変更
print(a11) # 当然13
print(a12) # ??? （同じアドレスを指しているはずだが？）
```

::: {.fragment}

- 結果： **12**
- `a11 = 13` は「11 だった場所に 13 を書き込む」**ではない** — **看板の掛け替え**
- 代入 `x = ...` は**常に掛け替え**。共有していたのは3行目から5行目までの一瞬だけ
- 5行目以降、共有していた場所を指すのは a12 だけになる

```{python}
#| echo: false
#| fig-align: center
fig, axes = plt.subplots(1, 3, figsize=(12.5, 3.0))
ax = axes[0]; panel(ax, "a11 = 11;  a12 = 12")
tag(ax, 0.8, 3.0, "a11"); obj(ax, 3.2, 3.0, "11", "…832")
tag(ax, 0.8, 1.2, "a12"); obj(ax, 3.2, 1.2, "12", "…864")
link(ax, 1.45, 3.0, 2.6, 3.0); link(ax, 1.45, 1.2, 2.6, 1.2)

ax = axes[1]; panel(ax, "a11 = a12")
tag(ax, 0.8, 3.0, "a11"); obj(ax, 3.2, 3.0, "11", "…832", faded=True)
tag(ax, 0.8, 1.2, "a12"); obj(ax, 3.2, 1.2, "12", "…864")
link(ax, 1.45, 3.0, 2.7, 1.45, hot=True)
link(ax, 1.45, 1.2, 2.6, 1.2)
ax.text(4.0, 2.3, "同じ場所を指す", fontsize=9, color="#c0392b", ha="center")

ax = axes[2]; panel(ax, "a11 = 13")
tag(ax, 0.8, 3.0, "a11"); obj(ax, 3.2, 3.0, "13", "…896", hot=True)
tag(ax, 0.8, 1.2, "a12"); obj(ax, 3.2, 1.2, "12", "…864")
link(ax, 1.45, 3.0, 2.6, 3.0, hot=True); link(ax, 1.45, 1.2, 2.6, 1.2)
ax.text(3.2, 3.9, "新しい場所に新しい値", fontsize=9, color="#c0392b", ha="center")
plt.tight_layout()
plt.show()
```

:::


## リストの代入（罠）

数値でやった操作をリストでやると...

```python
odd1 = [1, 3]
odd2 = [5, 7]
odd1 = odd2
odd1[0] = 9
print(odd2) # ???
```

::: {.fragment}

- 結果： **[9, 7]**
- `odd1[0] = 9` は看板の掛け替え**ではない** — 指している先の**中身の変更**
- 看板は2枚とも同じリストを指したまま → `odd2` から見ても変わる

```{python}
#| echo: false
#| fig-align: center
fig, axes = plt.subplots(1, 3, figsize=(12.5, 3.0))
ax = axes[0]; panel(ax, "odd1 = [1, 3];  odd2 = [5, 7]")
tag(ax, 0.8, 3.0, "odd1"); obj(ax, 3.2, 3.0, "[1, 3]", "…264")
tag(ax, 0.8, 1.2, "odd2"); obj(ax, 3.2, 1.2, "[5, 7]", "…120")
link(ax, 1.5, 3.0, 2.45, 3.0); link(ax, 1.5, 1.2, 2.45, 1.2)

ax = axes[1]; panel(ax, "odd1 = odd2")
tag(ax, 0.8, 3.0, "odd1"); obj(ax, 3.2, 3.0, "[1, 3]", "…264", faded=True)
tag(ax, 0.8, 1.2, "odd2"); obj(ax, 3.2, 1.2, "[5, 7]", "…120")
link(ax, 1.5, 3.0, 2.55, 1.45, hot=True)
link(ax, 1.5, 1.2, 2.45, 1.2)

ax = axes[2]; panel(ax, "odd1[0] = 9")
tag(ax, 0.8, 3.0, "odd1")
obj(ax, 3.2, 1.2, "[9, 7]", "…120（同じ場所のまま！）", hot=True)
tag(ax, 0.8, 1.2, "odd2")
link(ax, 1.5, 3.0, 2.55, 1.45)
link(ax, 1.5, 1.2, 2.45, 1.2)
ax.text(3.2, 3.3, "中身を書き換えた\n→ odd2 から見ても変わる", fontsize=9,
        color="#c0392b", ha="center")
plt.tight_layout()
plt.show()
```

:::


## クイズ：どっちなら odd2 は無事？

ほぼ同じに見える2つのコード。`print(odd2)` は何と出る？

:::: {.columns}

::: {.column width="50%"}
```python
# A
odd1 = [1, 3]
odd2 = odd1
odd1 = [9, 7]
print(odd2)  # ???
```
:::

::: {.column width="50%"}
```python
# B
odd1 = [1, 3]
odd2 = odd1
odd1[0] = 9
print(odd2)  # ???
```
:::

::::

::: {.fragment}

- A： **[1, 3]** — `odd1 = [9, 7]` は**看板の掛け替え**。odd2 は無傷
- B： **[9, 7]** — `odd1[0] = 9` は**中身の変更**。odd2 も巻き込まれる

```{python}
#| echo: false
#| fig-align: center
fig, axes = plt.subplots(1, 2, figsize=(10.5, 2.9))
ax = axes[0]; panel(ax, "A の実行後（掛け替え）", mono=False)
tag(ax, 0.8, 3.0, "odd1"); obj(ax, 3.2, 3.0, "[9, 7]", "…776（新しい場所）", hot=True)
tag(ax, 0.8, 1.2, "odd2"); obj(ax, 3.2, 1.2, "[1, 3]", "…264")
link(ax, 1.5, 3.0, 2.45, 3.0, hot=True); link(ax, 1.5, 1.2, 2.45, 1.2)

ax = axes[1]; panel(ax, "B の実行後（中身の変更）", mono=False)
tag(ax, 0.8, 3.0, "odd1"); tag(ax, 0.8, 1.2, "odd2")
obj(ax, 3.2, 2.1, "[9, 7]", "…264（同じ場所のまま）", hot=True)
link(ax, 1.5, 3.0, 2.5, 2.3); link(ax, 1.5, 1.2, 2.5, 1.9)
plt.tight_layout()
plt.show()
```

:::


## ルールは2本だけ

| | 操作 | 何が起きる |
|---|---|---|
| **ルール1** | `x = ...`（代入） | **看板の掛け替え**。型に関係なく常に。コピーは起きない |
| **ルール2** | `x[0] = ...` や `.append()` | 指している先の**中身を変更**。できるのは**ミュータブル**だけ |

- 「リストだけ変な動きをする」のではない — リストは**起こせる操作（ルール2）が1つ多い**だけ
- 迷ったら「これは掛け替え？　中身の変更？」と聞けば、すべてのクイズが解ける

::: {.callout-tip}
この2本で、冒頭の事件も説明できる。あと少し道具（`id()`）を足してから戻ろう。
:::


## `id()` で住所を確認（数値）

`id()` 関数で、変数が指しているアドレスを調べられる。

```python
a11 = 11
a12 = 12
print(id(a11)) # 2083050422832
print(id(a12)) # 2083050422864
a11 = a12
print(id(a11)) # 2083050422864。a12と同じ場所
a11 = 13
print(id(a11)) # 2083050422896。違う場所を指している。
```

- 代入のたびに**指す先（アドレス）が変わっている** — ルール1（掛け替え）を `id()` で目撃できる
- `a11 = 13` をしても、`11` や `12` の場所は書き換わっていない

::: {.callout-warning}
## 豆知識：小さい整数は使い回される
- CPython は **-5〜256 の int をあらかじめ作って使い回す**
- だから `x = 11; y = 11` とすると `id(x) == id(y)` は **True** になる
- 「同じ値なら同じ場所を指すことがある」だけで、看板モデル自体は変わらない（高速化のための最適化）
:::

## `id()` で住所を確認（リスト）

リストでは、要素に代入してもアドレスが変わらない。

```python
odd1 = [1, 3]
odd2 = [5, 7]
print(id(odd1)) # 2738928019264
print(id(odd2)) # 2738927989120
odd1 = odd2
print(id(odd1)) # 2738927989120
print(id(odd2)) # 2738927989120
odd1[0] = 9    # odd2ではなくodd1を変更
print(odd1)    # [9, 7]
```

- `odd1 = odd2` 後、両者が**同じアドレスを指している**
- だから `odd1[0] = 9` が `odd2` にも**影響する**

## 「同じ場所？」を直接聞く：`is` 演算子

- `id() == id()` を毎回書くのは回りくどい — **`is`** で直接聞ける

```python
odd1 = [9, 7]
odd2 = odd1

print(odd1 is odd2)      # True  （同じ場所を指している）
print(odd1 == [9, 7])    # True  （中身が同じ）
print(odd1 is [9, 7])    # False （中身は同じでも別の場所）
```

- **`==` は中身の比較、`is` は場所の比較**
- 「この2枚の看板、同じ実体を指してる？」と聞きたいときは `is`

## ミュータブル / イミュータブル

値（オブジェクト）は2種類に分かれる

- **イミュータブル（[immutable]{.en}）**：中身を変更する手段が**存在しない**型
  - int str bool tuple 等
  - 「変えた」ように見える操作も、すべて**新しい値への掛け替え**（ルール1）
  - だからアドレスを意識しなくても事故が起きない
- **ミュータブル（[mutable]{.en}）**：中身を**その場で変更できる**型（ルール2が使える）
  - list、dict 等
  - 看板を何枚立てても実体は1つ — **共有に気づかないと事故になる**

| 分類 | 型 |
|---|---|
| イミュータブル | **`int` / `float` / `complex` / `bool` / `str` / `tuple` / `frozenset` / `bytes` / `None`** |
| ミュータブル | `list` `dict` `set` クラス など |


## 事件の解決：冒頭のコードに戻る

```python
def double_all(data):
    for i in range(len(data)):
        data[i] = data[i] * 2   # ← ルール2：中身の変更！
    return data

scores = [10, 20, 30]
result = double_all(scores)     # ← 関数に渡す＝ data という看板を立てる（ルール1）
print(result)  # [20, 40, 60]
print(scores)  # [20, 40, 60]
```

- 関数に渡しても**コピーは起きない**。引数 `data` は `scores` と同じリストを指す看板（ルール1）
- 中で `data[i] = ...` と**中身を変更**したので（ルール2）、`scores` から見ても壊れていた
- 約束どおり、**図1枚**で説明できる：

```{python}
#| echo: false
#| fig-align: center
fig, ax = plt.subplots(figsize=(7.5, 2.8))
ax.set_xlim(0, 7.5); ax.set_ylim(0, 4); ax.axis("off")
tag(ax, 1.0, 3.2, "scores")
tag(ax, 1.0, 2.0, "data")
ax.text(1.0, 1.52, "（関数の引数）", fontsize=8.5, color="#7d6608", va="top",
        ha="center")
tag(ax, 1.0, 0.8, "result")
obj(ax, 5.4, 2.0, "[20, 40, 60]", "…480")
ax.text(5.4, 0.9, "全員が同じリストを指す（看板3枚）", ha="center", fontsize=10,
        color="#c0392b", fontweight="bold")
for y in (3.2, 2.0, 0.8):
    link(ax, 1.75, y, 4.35, 2.0 + (y - 2.0) * 0.18)
plt.tight_layout()
plt.show()
```


## ミュータブルの罠を回避する：コピー

- リストを**別のアドレスに新たに複製**したいときは `.copy()` を使う
   - 「この場面を残したい」ときにセーブデータを別スロットへコピーするイメージ
   - 単に `=` で代入しても複製されない（看板が増えるだけ）
- 冒頭の事故も、**渡す側の1行**で防げる：

```python
result = double_all(scores.copy())   # 複製を渡す → scores は無傷
```

```python
odd1 = [1, 3]

# NG: 同じアドレスを共有 → 両方が変わる
odd2 = odd1
odd2[0] = 9
print(odd1)  # [9, 3] ← 意図せず変わってしまう

# OK: .copy() で別アドレスの複製を作る
odd3 = odd1.copy()
odd3[0] = 99
print(odd1)  # [9, 3] ← 変わらない
print(odd3)  # [99, 3]

# （参考）全スライス [:] でも同じ結果
odd4 = odd1[:]
print(id(odd1) == id(odd4))  # False（別アドレス）
```

```{python}
#| echo: false
#| fig-align: center
fig, axes = plt.subplots(1, 2, figsize=(10.5, 2.9))
ax = axes[0]; panel(ax, "odd2 = odd1 （共有）", mono=False)
tag(ax, 0.8, 3.0, "odd1"); tag(ax, 0.8, 1.2, "odd2")
obj(ax, 3.2, 2.1, "[9, 3]", "…264")
link(ax, 1.5, 3.0, 2.5, 2.3); link(ax, 1.5, 1.2, 2.5, 1.9)
ax.text(3.2, 3.3, "1つのリストに看板2枚", fontsize=9.5, color="#b9770e",
        ha="center")

ax = axes[1]; panel(ax, "odd3 = odd1.copy() （複製）", mono=False)
tag(ax, 0.8, 3.0, "odd1"); obj(ax, 3.2, 3.0, "[9, 3]", "…264")
tag(ax, 0.8, 1.2, "odd3"); obj(ax, 3.2, 1.2, "[99, 3]", "…776（別の場所）", hot=True)
link(ax, 1.5, 3.0, 2.45, 3.0); link(ax, 1.5, 1.2, 2.45, 1.2)
plt.tight_layout()
plt.show()
```

## 演習 {.badge-practice}

::: {.prompt}
「箱ではなく看板」の変数モデルを、次の4つの実験で確かめよ。
:::

1. **紙に看板図を描く**：次のコードを1行ずつ、看板と矢印で描きながら追い、最後の出力を**予想してから**実行して答え合わせ

   ```python
   a = [1, 2]
   b = a
   a.append(3)   # 掛け替え？ 中身の変更？
   a = [9]       # 掛け替え？ 中身の変更？
   print(b)      # ???
   ```

2. `dict` がミュータブルであることを `id()`（または `is`）で確認
3. `list` と同じようにアクセスできる `str` は、ミュータブルかイミュータブルかを確認
4. 上記の例で `odd1[0]` のアドレスがどうなるかを確認（`list` はミュータブルだが、`list` の要素は `int` なのでイミュータブルのはず）

```{python}
#| echo: true
#| eval: false
#| code-fold: true
#| code-summary: "解答例を見る"
a = [1, 2]
b = a
a.append(3)     # 中身の変更（ルール2）→ b からも見える
a = [9]         # 掛け替え（ルール1）→ b は無傷
print(b)        # (1) [1, 2, 3]

d = {"名前": "田中"}
e = d
d["点数"] = 80
print(e)        # (2) 点数が増えて見える → dict はミュータブル
print(d is e)   #     True（同じ場所を指したまま）

s = "abc"
# s[0] = "x"    # (3) TypeError → str はイミュータブル（中身を変える手段がない）

odd1 = [9, 3]
print(id(odd1[0]))   # (4) 要素が指す int のアドレス
odd1[0] = 99         #     リスト側の枠が「別の int」を指すだけ（int 自体は不変）
print(id(odd1[0]))   #     アドレスが変わっている
```

::: {.callout-tip}
## 矢印が動くのを見たいとき
[Python Tutor](https://pythontutor.com/) にコードを貼ると、看板（名前）と矢印が
1行ごとに動く様子をステップ実行で確認できます。今日の図と同じ世界観です。
:::

## まとめ

- 変数は値そのものではなく、**値が置かれたアドレスを指す看板**
- **ルール1**：代入 `x = ...` は型に関係なく**常に看板の掛け替え**。コピーは起きない
- **ルール2**：**ミュータブル**（list / dict 等）だけが**中身をその場で変更**できる — 共有中の変更は全員に見える
- イミュータブル（int / str / bool / tuple 等）は中身を変える手段がないので事故が起きない
- 事故を防ぐには **`.copy()`** で別の実体を渡す。確認の道具は **`id()`**（住所）と **`is`**（同じ場所か）
